import type { LayoutData } from '../../../routes/[locale=locale]/(app)/$types'

export const mockLayoutData: LayoutData = {
  supabase: null,
  user: null,
  authResponse: null,
  t: null,
  locale: 'en',
}
