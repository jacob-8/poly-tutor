import type { LayoutData } from '../../../routes/[lang=locale]/(app)/$types'

export const mockLayoutData: LayoutData = {
  supabase: null,
  user: null,
  authResponse: null,
  t: null,
  locale: 'en',
}
