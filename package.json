{
  "name": "poly-tutor-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F site dev",
    "prod": "pnpm -F site prod",
    "build": "pnpm -F site build",
    "preview": "pnpm -F site preview",
    "postinstall": "simple-git-hooks",
    "check": "pnpm -F site check",
    "check:watch": "pnpm -F site check-watch",
    "test": "vitest",
    "bench": "vitest bench",
    "e2e": "pnpm -F site test:e2e:ui",
    "lint": "eslint --cache --quiet",
    "lint:fix": "eslint --cache --fix",
    "reset-db": "pnpm -F site reset-db",
    "check-packages": "pnpm update --interactive --recursive --latest"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "6.19.1",
    "@typescript-eslint/parser": "6.19.1",
    "eslint": "^8.56.0",
    "eslint-define-config": "^2.1.0",
    "eslint-plugin-svelte": "^2.35.0",
    "globals": "^13.24.0",
    "simple-git-hooks": "^2.9.0",
    "svelte": "^4.2.12",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  },
  "type": "module",
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,svelte}": [
      "eslint --cache --fix"
    ]
  },
  "msw": {
    "workerDirectory": "site\\static"
  }
}